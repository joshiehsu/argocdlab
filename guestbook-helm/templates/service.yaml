apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
  namespace: default
spec:
  type: {{ .Values.service.type }}
  selector:
    app: guestbook-ui
  ports:
    - port: {{ .Values.service.port }}          
      targetPort: {{ .Values.service.targetPort }} 
